if SHIELD_HALF_PLANK_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Half Plank"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_HALF_PLANK_LEFT || SHIELD_HALF_PLANK_RIGHT

config ZMK_SPLIT
    default y

endif